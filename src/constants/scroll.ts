/** Reels/TikTok-style: the doom-scroll feed (and anything overlaid on top
 * of it, like the comment drawer) stays a narrow phone-width column,
 * centered with letterbox bars on wider screens, rather than stretching
 * full-bleed to whatever the browser window happens to be. */
export const SCROLL_PHONE_WIDTH = 480
